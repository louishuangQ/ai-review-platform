/** Replace this explicit local adapter with versioned HTTP APIs when deployed. */
export {
  scoringPolicy,
  previewScore,
  serviceDefaults,
  loadConfiguration,
  saveConfiguration,
  validateConfiguration,
  batchSamples,
} from "../mocks/operations";
export type { Configuration } from "../mocks/operations";
